<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>[report title]</title>

  <style>
    /*
      Print settings:
      - Page: A4, portrait
      - Page margins: 1in on all sides
      - Body: Times New Roman, 12pt, #131313
      - Tables: responsive and at least as wide as the printable page area
    */
    @page {
      size: A4 landscape;
      margin: 1in;
    }

    * {
      box-sizing: border-box;
    }

    html,
    body {
      margin: 0;
      padding: 0;
      background: #ffffff;
      color: #131313;
      font-family: "Times New Roman", "Liberation Serif", serif;
      font-size: 12pt;
      line-height: 1.1583; /* Word default: 278/240 */
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }

    h1,
    h2 {
      font-family: "Times New Roman", "Liberation Serif", serif;
      font-weight: 700;
      break-after: avoid;
      page-break-after: avoid;
    }

    h1 {
      margin: 12pt 0;
      font-size: 16pt;
      line-height: 18pt;
      text-align: center;
    }

    h2 {
      margin: 36pt 0 12pt;
      font-size: 14pt;
      line-height: 14pt;
      text-align: left;
    }

    table {
      width: auto;
      min-width: 100%;
      margin: 0;
      border: 0;
      border-collapse: collapse;
      border-spacing: 0;
      table-layout: auto;
      font-size: 12pt;
      line-height: 12pt;
    }

    th,
    td {
      height: 26.3pt;
      padding: 0 5.4pt; /* Word normal-table left/right cell margin: 108 twips */
      border: 0;
      vertical-align: middle;
      text-align: left;
      overflow-wrap: anywhere;
    }

    th {
      font-weight: 700;
    }

    .align-right {
      text-align: right;
    }

    :root {
      --income-color: #16863d;
      --expense-color: #dc2626;
    }

    .income,
    .income-green {
      color: var(--income-color);
    }

    .expense,
    .expense-red {
      color: var(--expense-color);
    }

    /* Color all income and expense content in the summary tables. */
    .categories-table th:first-child,
    .categories-table tbody td:nth-child(1),
    .categories-table tbody td:nth-child(2),
    .monthly-table th:nth-child(2),
    .monthly-table tbody td:nth-child(2),
    .average-table th:nth-child(2),
    .average-table tbody td:nth-child(2) {
      color: var(--income-color);
    }

    .categories-table th:last-child,
    .categories-table tbody td:nth-child(3),
    .categories-table tbody td:nth-child(4),
    .monthly-table th:nth-child(3),
    .monthly-table tbody td:nth-child(3),
    .average-table th:nth-child(3),
    .average-table tbody td:nth-child(3) {
      color: var(--expense-color);
    }

    /* Apply one of these classes to a transaction amount based on its type. */
    .transactions-table .income-amount {
      color: var(--income-color);
    }

    .transactions-table .expense-amount {
      color: var(--expense-color);
    }

    /* First table: report-period metadata. */
    .period-table {
      font-size: 14pt;
      line-height: 16pt;
    }

    .period-table td {
      height: 28.1pt;
    }

    /* Word style: “List Table 1 Light Accent 1”. */
    .accent-list thead th {
      border-bottom: 0.5pt solid #45b0e1;
    }

    .accent-list tbody tr:nth-child(odd) td {
      background: #c1e4f5;
    }

    .accent-list tbody tr:last-child td {
      border-top: 0.5pt solid #45b0e1;
    }

    /* Category table has only one vertical separator, at the 50% point. */
    .categories-table .mid-separator {
      border-left: 0.5pt solid #45b0e1;
    }

    /* Month table has separators between all three columns. */
    .monthly-table th + th,
    .monthly-table td + td {
      border-left: 0.5pt solid #45b0e1;
    }

    /* The average table uses the same accent-list styling as the monthly table. */

    .transactions-table thead {
      display: table-header-group;
    }

    .transactions-table tr {
      break-inside: avoid;
      page-break-inside: avoid;
    }

    .transactions-table tbody tr:last-child td {
      border-top: 0;
      border-bottom: 0.5pt solid #45b0e1;
    }

    /* Prevent an empty template row from adding visible text while retaining Word row height. */
    .empty-cell::after {
      content: "\00a0";
    }

    /* Keep small summary tables with their headings when the report grows. */
    .keep-together {
      break-inside: avoid;
      page-break-inside: avoid;
    }
  </style>
</head>
<body>
  <h1>[report title]</h1>

  [report period section]

  [totals section]

  [totals by category section]

  [totals by month section]

  [averages section]

  [transactions section]
</body>
</html>